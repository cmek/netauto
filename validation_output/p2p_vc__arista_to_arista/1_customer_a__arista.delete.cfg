router bgp 65001
   vlan-aware-bundle SO101010
      no vlan 100
interface Vxlan1
   no vxlan vlan 100 vni 5000
interface Ethernet6
   switchport trunk allowed vlan remove 100
no vlan 100
router bgp 65001
   no vlan-aware-bundle SO101010
